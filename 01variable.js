//1. Create a variable.js file and declare variables and assign string, boolean, undefined and null data types, Display all the value with there type.

let myName = "Zia";
let engineer = true;
let role;
let city = null;

console.log(`Value is ${myName} and type is ${typeof myName}`);
console.log(`Value is ${engineer} and type is ${typeof(engineer)}`)
console.log(`Value is ${role} and type is ${typeof role}`);
console.log(`Value is ${city} and type is ${typeof(city)}`)